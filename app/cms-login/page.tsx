import Link from "next/link"; import { LockKeyhole } from "lucide-react"; import { loginCms } from "./actions";
export default async function Login({searchParams}:{searchParams:Promise<{erro?:string;returnTo?:string}>}){const p=await searchParams;return <main className="cms-login-page"><form action={loginCms} className="cms-login-card"><Link className="brand" href="/"><span className="brand-mark">AR</span><span className="brand-name">André Ribeiro <small>Tattoo</small></span></Link><LockKeyhole size={28}/><div><p>Painel administrativo</p><h1>Acessar CMS</h1><span>Entre com os dados configurados na Vercel.</span></div>{p.erro&&<strong className="login-error">Usuário ou senha incorretos.</strong>}<input type="hidden" name="returnTo" value={p.returnTo||"/admin"}/><label>Usuário<input name="user" autoComplete="username" required/></label><label>Senha<input name="password" type="password" autoComplete="current-password" required/></label><button>Entrar no painel</button><Link href="/">Voltar ao site</Link></form></main>}
