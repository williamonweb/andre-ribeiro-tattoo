"use server";
import { cookies } from "next/headers"; import { redirect } from "next/navigation"; import { CMS_COOKIE,makeCmsToken } from "@/lib/admin";
export async function loginCms(fd:FormData){const user=String(fd.get("user")||"").trim(),password=String(fd.get("password")||"");const expectedUser=process.env.ADMIN_USER||"admin",expectedPassword=process.env.ADMIN_PASSWORD||"";const returnTo=String(fd.get("returnTo")||"/admin");if(!expectedPassword||user!==expectedUser||password!==expectedPassword)redirect(`/cms-login?erro=1&returnTo=${encodeURIComponent(returnTo)}`);(await cookies()).set(CMS_COOKIE,makeCmsToken(user,password),{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",path:"/",maxAge:60*60*12});redirect(returnTo.startsWith("/admin")?returnTo:"/admin");}
