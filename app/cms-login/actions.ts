"use server";
import { cookies } from "next/headers"; import { redirect } from "next/navigation"; import { CMS_COOKIE,getCmsSettings,makeCmsToken,verifyCmsCredentials } from "@/lib/admin";
export async function loginCms(fd:FormData){const user=String(fd.get("user")||"").trim(),password=String(fd.get("password")||"");const returnTo=String(fd.get("returnTo")||"/admin");if(!(await verifyCmsCredentials(user,password)))redirect(`/cms-login?erro=1&returnTo=${encodeURIComponent(returnTo)}`);const settings=await getCmsSettings();(await cookies()).set(CMS_COOKIE,makeCmsToken(user,settings?.session_version||1),{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",path:"/",maxAge:60*60*12});redirect(returnTo.startsWith("/admin")?returnTo:"/admin");}
