import { AdminHeader, SavedNotice } from "@/components/admin-shell"; import { EditorCard, PageHeaderFields, SaveButton } from "@/components/admin-fields"; import { defaultTestimonials,getContent } from "@/lib/content"; import { saveTestimonials } from "../actions";
export default async function Page({searchParams}:{searchParams:Promise<{saved?:string}>}){const [data,params]=await Promise.all([getContent("testimonials",defaultTestimonials),searchParams]);return <><AdminHeader eyebrow="Conteúdo" title="Depoimentos" description="Troque os exemplos por avaliações reais dos clientes."/><SavedNotice saved={params.saved}/><form action={saveTestimonials} className="admin-card"><PageHeaderFields data={data}/>{data.items.map((item,i)=><EditorCard number={i+1} key={i}><label>Nome do cliente<input name={`name_${i}`} defaultValue={item.name}/></label><label>Estilo da tatuagem<input name={`style_${i}`} defaultValue={item.style}/></label><label className="full">Depoimento<textarea name={`quote_${i}`} rows={4} defaultValue={item.quote}/></label></EditorCard>)}<SaveButton/></form></>}
