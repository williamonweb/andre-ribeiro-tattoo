import { AdminHeader,SavedNotice } from "@/components/admin-shell"; import { EditorCard,PageHeaderFields,SaveButton } from "@/components/admin-fields"; import { defaultSpecialties,getContent } from "@/lib/content"; import { saveSpecialties } from "../actions";
export default async function Page({searchParams}:{searchParams:Promise<{saved?:string}>}){const [data,params]=await Promise.all([getContent("specialties",defaultSpecialties),searchParams]);return <><AdminHeader eyebrow="Conteúdo" title="Especialidades" description="Edite estilos, explicações e a imagem de cada quadro."/><SavedNotice saved={params.saved}/><form action={saveSpecialties} className="admin-card"><PageHeaderFields data={data}/>{data.items.map((item,i)=><EditorCard number={i+1} key={i}><div className="specialty-admin-thumb" style={{backgroundImage:`url(${item.imageUrl||"/instagram-reference.png"})`,backgroundPosition:item.position}}/><label>Nome do estilo<input name={`title_${i}`} defaultValue={item.title}/></label><label>Legenda do exemplo<input name={`example_${i}`} defaultValue={item.example}/></label><label className="full">Explicação<textarea name={`text_${i}`} rows={3} defaultValue={item.text}/></label><label className="full">Trocar imagem do quadro<input name={`image_${i}`} type="file" accept="image/jpeg,image/png,image/webp"/><small>Se não escolher uma nova imagem, a atual será mantida.</small></label><input type="hidden" name={`position_${i}`} value={item.position}/></EditorCard>)}<SaveButton/></form></>}
